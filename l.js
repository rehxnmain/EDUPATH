const $=s=>document.querySelector(s),$$=s=>document.querySelectorAll(s);function toast(t){let x=$('#toast');x.textContent=t;x.classList.add('show');setTimeout(()=>x.classList.remove('show'),2400)}$$('.start-course').forEach(b=>b.onclick=e=>{let c=e.target.closest('.course');c.classList.add('complete');e.target.textContent='✓ In progress';toast(`${c.dataset.course} added to your active learning plan!`)});$$('.bookmark').forEach(b=>b.onclick=()=>{b.classList.toggle('saved');b.textContent=b.classList.contains('saved')?'♥':'♡';toast(b.classList.contains('saved')?'Course saved for later.':'Course removed from saved list.')});$$('.tabs button').forEach(b=>b.onclick=()=>{$$('.tabs button').forEach(x=>x.classList.remove('selected'));b.classList.add('selected');toast(`${b.textContent.trim()} selected`)});$('#filter').onclick=()=>toast('Filters are ready: difficulty, duration, provider, and certificate.');